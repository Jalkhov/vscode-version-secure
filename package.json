{
  "name": "version-secure",
  "displayName": "Version Secure",
  "description": "Monitorea y verifica coherencia de versiones entre archivos de proyecto",
  "version": "1.0.3",
  "icon": "icon.png",
  "publisher": "jalkhov",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jalkhov/vscode-version-secure.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other",
    "Visualization"
  ],
  "keywords": [
    "version",
    "versioning",
    "sync",
    "check"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "versionChecker.check",
        "title": "Check Versions",
        "icon": "$(sync)"
      },
      {
        "command": "versionChecker.showDetails",
        "title": "Show Details",
        "icon": "$(list-unordered)"
      },
      {
        "command": "versionChecker.toggle",
        "title": "Toggle Version Checker",
        "icon": "$(eye)"
      }
    ],
    "configuration": {
      "title": "Version Checker",
      "properties": {
        "versionChecker.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Habilitar/deshabilitar Version Checker para este workspace"
        },
        "versionChecker.files": {
          "type": "object",
          "default": {},
          "description": "Archivos a monitorear: {\"archivo.ext\": {\"patterns\": [\"regex\"]}}"
        },
        "versionChecker.checkOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Verificar al guardar archivos"
        },
        "versionChecker.checkInterval": {
          "type": "number",
          "default": 0,
          "description": "Intervalo de verificación en ms (0 = desactivado)"
        },
        "versionChecker.statusBarPriority": {
          "type": "number",
          "default": 1000,
          "description": "Prioridad en status bar"
        }
      }
    }
  }
}
